services:
  homeassistant-bridge:
    image: ghcr.io/maxhayim/meshmonitor-bridge-homeassistant:latest
    container_name: homeassistant-bridge
    restart: unless-stopped
    env_file:
      - homeassistant-bridge.env
